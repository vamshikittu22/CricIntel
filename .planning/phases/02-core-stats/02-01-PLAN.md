---
phase: 02-core-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/pages/Index.tsx]
autonomous: true

must_haves:
  truths:
    - "Home page displays recent matches from Supabase"
    - "Recent matches show match date, venue, teams, and result"
  artifacts:
    - path: "src/pages/Index.tsx"
      provides: "Home page with recent matches section"
      contains: "useRecentMatches"
  key_links:
    - from: "src/pages/Index.tsx"
      to: "matches table"
      via: "useRecentMatches hook"
      pattern: "useRecentMatches"
---

<objective>
Add recent matches display to home page to satisfy HOME-02 requirement.

Purpose: Users should see recent cricket matches on the home page with basic match info.
Output: Home page shows recent matches section with date, teams, venue, result.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/pages/Index.tsx
@src/lib/hooks/usePlayers.ts
</context>

<tasks>

<task type="auto">
  <name>Add recent matches section to home page</name>
  <files>src/pages/Index.tsx</files>
  <action>
    Add recent matches display to the Index.tsx home page.

    1. Import useRecentMatches hook from @/lib/hooks/usePlayers
    2. Add a "Recent Matches" section below the "Browse by Team" section
    3. Display matches showing:
       - Match date
       - Team 1 vs Team 2 (with flags)
       - Venue
       - Result (winner, margin)
    4. Show format badge (ODI/T20I/Test)
    5. Make matches clickable - navigate to match details page (to be created in plan 02)

    Use the existing Card components and styling patterns from MatchHistory.tsx.
  </action>
  <verify>npm run build passes, home page shows recent matches</verify>
  <done>Home page displays recent matches from Supabase with date, teams, venue, result</done>
</task>

</tasks>

<verification>
1. Run: npm run dev
2. Visit home page
3. Verify recent matches section appears with real data from matches table
</verification>

<success_criteria>
- [ ] Home page displays recent matches from Supabase
- [ ] Each match shows date, teams, venue, format, result
- [ ] Clicking a match navigates to match details (or shows placeholder for now)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-stats/02-01-SUMMARY.md`
</output>
