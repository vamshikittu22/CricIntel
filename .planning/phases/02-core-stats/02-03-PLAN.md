---
phase: 02-core-stats
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified: [src/pages/MatchDetails.tsx, src/App.tsx]
autonomous: true

must_haves:
  truths:
    - "Match details page shows scorecard with batting figures"
    - "Scorecard shows runs, balls, fours, sixes, dismissal for each player"
    - "Bowling figures shown for each bowler"
  artifacts:
    - path: "src/pages/MatchDetails.tsx"
      provides: "Match details with full scorecard"
      contains: "match_player_stats query"
  key_links:
    - from: "src/pages/Matches.tsx"
      to: "src/pages/MatchDetails.tsx"
      via: "navigate(/match/${id})"
      pattern: "match/"
---

<objective>
Create match details page with full scorecard to satisfy MATCH-03 requirement.

Purpose: Users should be able to view detailed scorecard for any match showing all players' batting and bowling stats.
Output: New /match/:id route showing complete match scorecard.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/MatchHistory.tsx
@src/pages/Matches.tsx
@src/lib/hooks/usePlayers.ts
</context>

<tasks>

<task type="auto">
  <name>Create match details page with scorecard</name>
  <files>src/pages/MatchDetails.tsx</files>
    <action>
    Create a new MatchDetails.tsx page that displays full match scorecard.

    Features:
    1. Get match_id from URL params
    2. Query matches table for match info (date, venue, teams, result)
    3. Query match_player_stats for all players in the match
    4. Split by team (team1 and team2)
    5. Display batting scorecard:
       - Player name
       - runs, balls faced
       - fours, sixes
       - dismissal (how out)
       - strike rate
    6. Display bowling figures:
       - Overs, maidens, runs, wickets
       - Economy rate
    7. Show fall of wickets if available
    8. Show extras summary (wides, no-balls, byes, leg-byes)

    Use Table components similar to MatchHistory.tsx.
    Style with Cards for each innings.
    </action>
    <verify>npm run build passes, /match/:id shows scorecard</verify>
    <done>Match details page shows full batting and bowling scorecard</done>
</task>

<task type="auto">
  <name>Add match details route to App.tsx</name>
  <files>src/App.tsx</files>
    <action>
    Add the MatchDetails route to React Router in App.tsx.

    Add import:
    ```
    import MatchDetails from "./pages/MatchDetails";
    ```

    Add route:
    ```
    <Route path="/match/:id" element={<MatchDetails />} />
    ```
    </action>
    <verify>Route /match/:id accessible</verify>
    <done>/match/:id route exists and loads MatchDetails component</done>
</task>

</tasks>

<verification>
1. Run: npm run dev
2. Navigate to a specific match from /matches page
3. Verify scorecard displays with batting and bowling figures
4. Verify all players in match are listed
</verification>

<success_criteria>
- [ ] Match details page shows match info (date, venue, teams, result)
- [ ] Batting scorecard shows all players with runs, balls, dismissals
- [ ] Bowling figures show overs, maidens, runs, wickets, economy
- [ ] Both teams displayed in separate sections
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-stats/02-03-SUMMARY.md`
</output>
