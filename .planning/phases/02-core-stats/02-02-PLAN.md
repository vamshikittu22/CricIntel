---
phase: 02-core-stats
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/pages/Matches.tsx, src/App.tsx]
autonomous: true

must_haves:
  truths:
    - "Matches page lists all matches from matches table"
    - "Format filter tabs work (All, ODI, T20I, Test)"
    - "Matches display date, teams, venue, result"
  artifacts:
    - path: "src/pages/Matches.tsx"
      provides: "Global match listing page"
      contains: "matches table query"
    - path: "src/App.tsx"
      provides: "Route for /matches"
      contains: "Matches"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/Matches.tsx"
      via: "React Router route"
      pattern: "path=\"/matches\""
---

<objective>
Create global match listing page with format filtering to satisfy MATCH-01 and MATCH-02 requirements.

Purpose: Users should be able to browse all matches, filter by format, and see basic match info.
Output: New /matches route showing all matches with ODI/T20I/Test filter tabs.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/MatchHistory.tsx
@src/pages/Index.tsx
@src/lib/hooks/usePlayers.ts
</context>

<tasks>

<task type="auto">
  <name>Create matches listing page</name>
  <files>src/pages/Matches.tsx</files>
  <action>
    Create a new Matches.tsx page that displays all matches from the database.

    Features:
    1. Query matches table using supabase.from("matches")
    2. Format filter tabs: All, ODI, T20I, Test
    3. Display match cards showing:
       - Date (formatted)
       - Team 1 vs Team 2 (with flags)
       - Venue
       - Format badge
       - Result (winner + margin, or "Tie", "No Result")
    4. Sort by date descending (newest first)
    5. Add search/filter by team name
    6. Clicking a match navigates to match details page

    Use similar styling to MatchHistory.tsx and Index.tsx sections.
  </action>
  <verify>npm run build passes, /matches route shows matches</verify>
  <done>Matches page lists all matches from Supabase with format filtering</done>
</task>

<task type="auto">
  <name>Add matches route to App.tsx</name>
  <files>src/App.tsx</files>
    <action>
    Add the Matches route to React Router in App.tsx.

    Add import:
    ```
    import Matches from "./pages/Matches";
    ```

    Add route:
    ```
    <Route path="/matches" element={<Matches />} />
    ```
    </action>
    <verify>Route /matches accessible</verify>
    <done>/matches route exists and loads Matches component</done>
</task>

</tasks>

<verification>
1. Run: npm run dev
2. Navigate to /matches
3. Verify matches display with format tabs working
4. Verify search/filter by team works
</verification>

<success_criteria>
- [ ] /matches route displays all matches from database
- [ ] Format tabs filter matches correctly
- [ ] Match info (date, teams, venue, result) displays properly
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-stats/02-02-SUMMARY.md`
</output>
