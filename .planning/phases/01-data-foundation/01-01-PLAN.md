---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.env, supabase/migrations/20260311012310_f2732f64-878c-4b15-aacd-537d2910540c.sql]
autonomous: true
user_setup:
  - service: supabase
    why: "ETL requires service role key for database write access"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> Service Role Key (SECRET)"
    dashboard_config:
      - task: "Get service role key"
        location: "Supabase Dashboard -> Settings (cog icon) -> API -> ' section ->Service Role' Click 'Reveal' and copy secret"

must_haves:
  truths:
    - "ETL script runs without error when environment variables are set"
    - "Database tables are created in Supabase"
    - "Cricsheet JSON files can be imported via npm run import:cricsheet"
    - "Players, matches, and match_player_stats are populated after import"
  artifacts:
    - path: ".env"
      provides: "Environment variables for Supabase connection"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "supabase/migrations/20260311012310_f2732f64-878c-4b15-aacd-537d2910540c.sql"
      provides: "Database schema definitions"
      contains: "CREATE TABLE public.players"
    - path: "scripts/import-cricsheet.ts"
      provides: "ETL script for importing Cricsheet data"
      exports: ["main function"]
  key_links:
    - from: "scripts/import-cricsheet.ts"
      to: "supabase"
      via: "supabase-js client with service role key"
      pattern: "createClient.*SERVICE_ROLE_KEY"
    - from: ".env"
      to: "scripts/import-cricsheet.ts"
      via: "process.env.SUPABASE_SERVICE_ROLE_KEY"
      pattern: "process\\.env\\.SUPABASE_SERVICE_ROLE_KEY"
---

<objective>
Configure environment and run database migrations to prepare Supabase for data import.

Purpose: Phase 1 requires service role key for ETL writes, and database tables must exist before import.
Output: Supabase database with players, matches, match_player_stats, player_stats_summary, player_phase_stats tables
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@scripts/import-cricsheet.ts
@src/lib/cricsheet.ts
@src/integrations/supabase/types.ts
</context>

<tasks>

<task type="auto">
  <name>Add Supabase service role key to .env</name>
  <files>.env</files>
  <action>
    Add SUPABASE_SERVICE_ROLE_KEY to .env file. The user needs to get this from Supabase Dashboard.
    
    Add these lines to .env:
    ```
    SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
    ```
    
    IMPORTANT: The key must be the SERVICE ROLE secret (not the anon/public key). It starts with "eyJ..." and is much longer.
  </action>
  <verify>cat .env includes SUPABASE_SERVICE_ROLE_KEY line</verify>
  <done>.env contains SUPABASE_SERVICE_ROLE_KEY with a valid key value</done>
</task>

<task type="auto">
  <name>Run database migrations against Supabase</name>
  <files>supabase/migrations/20260311012310_f2732f64-878c-4b15-aacd-537d2910540c.sql</files>
  <action>
    Run migrations using Supabase CLI. The migration creates:
    - players table (text PK with Cricsheet registry ID)
    - matches table (text PK)
    - match_player_stats table (per-match batting/bowling stats)
    - player_stats_summary table (career aggregates)
    - player_phase_stats table (powerplay/middle/death overs)
    - player_vs_bowling_type table (future use)
    - RLS policies for public read access
    
    Run: npx supabase db push
  </action>
  <verify>npx supabase db push --db-url "postgresql://postgres:[YOUR-PASSWORD]@db.olxtkzrjplohtiqszazj.supabase.co:5432/postgres"</verify>
  <done>All 6 tables exist in Supabase with correct schema</done>
</task>

<task type="auto">
  <name>Verify ETL script runs successfully</name>
  <files>scripts/import-cricsheet.ts</files>
  <action>
    Run the ETL script to import Cricsheet data. Ensure environment variables are loaded.
    
    Run: npm run import:cricsheet
    
    The script will:
    1. Scan ./data folder for JSON files
    2. Parse each Cricsheet JSON
    3. Upsert players, matches, match_player_stats
    4. Compute and upsert career summaries
    
    If data/odis_json has files, they will be imported.
  </action>
  <verify>npm run import:cricsheet -- ./data/odis_json</verify>
  <done>Script completes without error, players and matches exist in Supabase</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Database tables and imported data</what-built>
  <how-to-verify>
    1. Go to Supabase Dashboard: https://supabase.com/dashboard/project/olxtkzrjplohtiqszazj
    2. Click "Table Editor" in left sidebar
    3. Verify these tables exist with rows:
       - players: should have player records
       - matches: should have match records  
       - match_player_stats: should have per-match stats
       - player_stats_summary: should have career aggregates
    4. Click on "players" table and check sample data
  </how-to-verify>
  <resume-signal>Type "approved" or describe any data issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. Query Supabase: SELECT COUNT(*) FROM players, matches, match_player_stats
2. Verify counts > 0
3. Check sample player has career stats: SELECT * FROM player_stats_summary LIMIT 5
</verification>

<success_criteria>
- [x] .env contains SUPABASE_SERVICE_ROLE_KEY
- [x] Database migrations ran successfully
- [x] ETL script runs without error
- [x] Data is populated in Supabase (players, matches, stats)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
