---
phase: 01-data-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [scripts/import-cricsheet.ts]
autonomous: true

must_haves:
  truths:
    - "player_phase_stats table gets populated with powerplay/middle/death phase data"
    - "TypeScript types accurately reflect database schema"
  artifacts:
    - path: "scripts/import-cricsheet.ts"
      provides: "Updated ETL with phase stats computation"
      contains: "player_phase_stats"
    - path: "src/integrations/supabase/types.ts"
      provides: "TypeScript types matching schema"
      contains: "player_phase_stats"
  key_links:
    - from: "scripts/import-cricsheet.ts"
      to: "player_phase_stats"
      via: "computeSummaries function extension"
      pattern: "player_phase_stats"
---

<objective>
Enhance ETL to compute phase-of-match statistics and verify TypeScript types match database schema.

Purpose: DATA-07 requires computing powerplay/middle/death overs stats per player, and SCHM-06 requires types to match schema.
Output: player_phase_stats table populated, TypeScript types verified
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
@scripts/import-cricsheet.ts
@src/integrations/supabase/types.ts
@supabase/migrations/20260311012310_f2732f64-878c-4b15-aacd-537d2910540c.sql
</context>

<tasks>

<task type="auto">
  <name>Add player_phase_stats computation to ETL</name>
  <files>scripts/import-cricsheet.ts</files>
  <action>
    Enhance the ETL script to compute phase-of-match statistics (powerplay, middle, death overs).
    
    The current ETL processes deliveries but doesn't track which over phase each delivery belongs to.
    Cricket phases are typically:
    - Powerplay: overs 1-6 (or 1-10 in some formats)
    - Middle: overs 7-16 (or 11-40)
    - Death: overs 17-20 (or 41-50)
    
    Add:
    1. A function computePhaseStats() that iterates through deliveries and tracks:
       - bat_runs, bat_balls, bat_fours, bat_sixes, bat_dismissals by phase
       - bowl_balls, bowl_runs, bowl_wickets, bowl_econ by phase
    2. Integration into the main flow after match_player_stats processing
    3. Upsert to player_phase_stats table
    
    Use overs count to determine format-specific phase boundaries:
    - T20I: powerplay 1-6, middle 7-16, death 17-20
    - ODI: powerplay 1-10, middle 11-40, death 41-50
    - Test: varies, use first 10 overs as powerplay
  </action>
  <verify>grep -n "player_phase_stats" scripts/import-cricsheet.ts</verify>
  <done>ETL script computes and upserts player_phase_stats for each player per format</done>
</task>

<task type="auto">
  <name>Verify TypeScript types match database schema</name>
  <files>src/integrations/supabase/types.ts</files>
  <action>
    Compare the TypeScript types in src/integrations/supabase/types.ts with the database schema in supabase/migrations.
    
    Verify:
    1. players table: id, name, country, gender, created_at
    2. matches table: id, format, match_date, venue, team1, team2, result, etc.
    3. match_player_stats: all batting/bowling fields
    4. player_stats_summary: career aggregate fields
    5. player_phase_stats: phase-specific fields
    
    If types are missing or incorrect, update src/integrations/supabase/types.ts to match the migration schema exactly.
  </action>
  <verify>Types exist for all 5 tables with correct field names</verify>
  <done>TypeScript types accurately reflect database schema</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Phase stats computation and type verification</what-built>
  <how-to-verify>
    1. After running import again, check Supabase:
       SELECT player_id, format, phase, bat_runs, bowl_wickets 
       FROM player_phase_stats 
       LIMIT 10
    2. Verify different phases (powerplay, middle, death) exist for players
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Query: SELECT COUNT(*) FROM player_phase_stats - should be > 0
2. Query: SELECT DISTINCT phase FROM player_phase_stats - should show powerplay, middle, death
</verification>

<success_criteria>
- [x] ETL computes player_phase_stats
- [x] TypeScript types match database schema
- [x] Phase stats have data in database
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-02-SUMMARY.md`
</output>
